Hello there,

You (or someone pretending to be you) have requested a new password for your Sender25 account. To choose a new password, please click the link below and you'll be able to create a new password and login to Sender25.

<%= Sender25.host_with_protocol %>/login/reset/<%= @user.password_reset_token %><%= @return_to ? "?return_to=#{ERB::Util.url_encode(@return_to)}" : '' %>

If you didn't request this, you can ignore this e-mail.

Thanks,

<%= Sender25::Config.smtp.from_name %>
<%= Sender25::Config.smtp.from_address %>
